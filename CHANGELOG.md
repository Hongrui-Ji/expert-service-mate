# 项目更新说明（Changelog）

用于记录每一次较大的功能调整、数据结构变更与接口变更，便于回忆与验收。

## 约定
- 每次合并到 main 的“大调整”都新增一段记录
- 尽量包含：功能点、影响范围、数据结构、接口、验收要点

## 2026-08-23（Excel 智能排班生成器）

### 功能变更
- 工作台新增“智能排班生成器”卡片，公开地址为 `/auto-schedule/`
- 支持上传 `.xlsx`、设置年月与节假日、使用共享排班规则、预览异常并下载新工作簿
- 支持可视化维护偏远城市、品牌规则、容量、间隔、窗口和平滑权重

### 技术实现
- 原单文件脚本拆为排班引擎、内存 Excel I/O、JSON 配置存储和 Streamlit 页面
- 常规及季度任务统一执行日/周容量硬约束，无法满足的任务进入“异常待处理”
- 共享配置使用部署目录外的单个 JSON 文件，不新增数据库
- 上传和结果只在当前会话内存中存在，不连接或写入现有业务 SQLite
- 新服务监听 `127.0.0.1:8503`，由 PM2 管理，Nginx 提供公开 WebSocket 反向代理

### 验收要点
- 无需登录可打开 `/auto-schedule/`
- 1106 行样表可完整运行并下载包含摘要、省份结果和异常 Sheet 的 Excel
- 已排结果不超过配置的日/周硬上限
- 配置保存后刷新和重启仍生效，上传与结果文件不落盘

---

## 2026-08-18（现场作业报告独立上线）

### 功能变更
- 新增公开的 PCO 现场作业报告生成工具，独立访问地址为 `/onsite-report/`
- 支持填写现场检查与作业记录、制剂/药剂使用记录、上传现场图片并导出中文 PDF
- 新页面不接入账号登录，也不在工作台展示入口

### 技术实现
- 新工具作为独立 Streamlit 进程运行于 `127.0.0.1:8502`，由 PM2 管理
- Nginx 为 `/onsite-report/` 提供独立 WebSocket 反向代理
- 复用现有报告工具的 Python 虚拟环境、中文字体和 Logo，不新增数据库或 API

### 验收要点
- 直接访问 `/onsite-report/` 可打开页面并下载生成的 PDF
- 8502 端口仅监听服务器本机
- 工作台、排班工具和终端监测报告行为不变

## 2026-06-06（工作台统一登录与账号管理迁移）

### 功能变更
- 登录入口从排班系统迁移到工作台 `/workspace/`，未登录用户只看到手机号 + 密码登录页
- 登录成功后才展示工作台工具入口，包括“专家排班工具”和“终端监测报告”
- 管理员登录工作台后可直接进行账号管理：
  - 查看账号列表
  - 新增账号
  - 编辑账号信息
  - 启用/禁用账号
  - 重置密码
  - 设置普通用户/管理员角色
- 普通用户登录工作台后只显示工具入口，不显示账号管理
- 排班系统 `/workspace/schedule/` 不再显示登录页，也不再显示账号管理；未登录直接访问会返回工作台登录

### 接口变更
- 新增认证接口：
  - `GET /api/auth/me`：校验当前 JWT 是否有效，并返回当前用户基础信息
- 既有账号管理接口保持不变，仍由管理员角色访问：
  - `GET /api/admin/users`
  - `POST /api/admin/users`
  - `PUT /api/admin/users/:id`

### 技术实现
- 工作台页 `landing_page/index.html` 从静态入口页升级为带登录态控制的工作台页面
- 工作台和排班系统共用 `localStorage.auth_user`，登录后进入排班系统无需二次登录
- 工作台刷新时通过 `/api/auth/me` 校验本地 Token，Token 过期、账号禁用或无效时自动回到登录状态
- 排班系统保留 API 请求鉴权与本地 Token 过期检测，但不再承担登录和账号管理 UI

### 影响范围
- 工作台入口：新增登录态判断、管理员账号管理、普通用户权限展示
- 排班系统：移除登录表单与账号管理模块，只保留排班日历和门店库相关功能
- 后端：新增 `/api/auth/me`，其余账号管理接口权限逻辑不变

### 验收要点
- 未登录访问 `/workspace/`：只显示登录页
- 管理员登录 `/workspace/`：显示工具入口 + 账号管理
- 普通用户登录 `/workspace/`：显示工具入口，不显示账号管理
- 未登录访问 `/workspace/schedule/`：返回工作台登录
- 登录后进入 `/workspace/schedule/`：直接进入排班系统，不需要二次登录
- 排班系统中不再出现“账号管理 / 新增账号”

## 2026-05-09（管理员排班 Excel 导出）

### 功能变更
- 新增管理员专属“导出当月 Excel”能力，可将当前选中专家在当前月份的排班表导出为 `.xlsx`
- 导出文件包含两个 Sheet：
  - `月历视图`：按“周一到周日”排列，单元格展示日期、门店名、城市，临时上门显示 `临时：`
  - `排班明细`：展示日期、星期、门店信息、专家、类型、状态、是否计入目标、临时原因
- 导出口径固定为“当前选中专家 + 当前页面月份”，与页面当前是月视图还是周视图无关
- 历史上已排入该月的门店，即使后续被归档，导出时仍保留其排班记录

### 技术实现
- 前端引入 `exceljs`，在浏览器端直接生成 Excel 文件，无新增后端导出接口
- 为减少首屏体积影响，`exceljs` 采用动态加载，仅在点击导出时按需下载
- 导出月历使用独立的周历矩阵生成函数，按月首日偏移计算整月网格

### 问题修复
- 修复首版 Excel 月历中“同样日期出现在多行”的问题
- 根因是导出月历的外层循环按天滑动，导致每一行都与上一行重叠 6 天
- 现已改为按周推进，确保每一行严格对应一整周，导出的月历恢复为标准 5 行或 6 行布局

### 影响范围
- 前端排班页：新增管理员导出按钮与导出中状态
- 前端构建：新增按需加载的 `exceljs` 导出 chunk
- 文档：已同步更新架构文档中的前端能力、数据流与权限说明

### 验收要点
- 仅管理员可见“导出当月 Excel”按钮，普通用户不显示
- 导出月历列顺序为 `周一 -> 周日`
- 相邻两行不再出现重复日期，每行均为连续 7 天
- 单元格中不显示“已完成”标记，但临时上门仍显示 `临时：`

## 2026-03-29（排班能力扩展：月度目标、临时上门、门店停用）

### 功能变更
- 排班日历默认视图改为周视图；登录/切换账号后默认展示当前账号主体专家的排班视图
- 管理员支持“门店月度目标次数”覆盖（可增减、可为 0），并影响当月待排门店与进度计算
- 专家支持自建“临时上门”（报修/复查等），默认不计入常规目标次数；管理员可选择计入
- 临时上门在日历中用橙色样式区分，并展示原因（title）
- 历史月份不显示待排门店列表（仅用于查看），避免新增/恢复门店导致历史月份出现待排
- 登录状态增强：本地检测 Token 过期并提示重新登录；服务端将过期/无效 Token 统一返回 401
- 门店库增强：新增“特殊需求”筛选；特殊需求仅管理员可修改

### 门店删除策略调整（物理删除 → 停用/归档，默认策略 A）
- 物理删除门店改为“停用门店（软删除）”，保留历史排班与审计价值
- 默认策略 A：若门店存在今天及未来的 planned 排班，停用会被阻止（返回 409），需先取消/改期后再停用
- 支持“恢复门店”，恢复后重新参与排班
- 门店停用后禁止创建新的排班/临时上门（返回 409）

### 数据库结构变更
- `stores` 新增字段：
  - `deleted_at`：停用时间；非空表示门店已停用/归档
  - `service_start_month`：门店从该月开始纳入常规待排计算（新增门店默认=创建当月）
  - `service_resume_month`：门店恢复后从该月开始重新纳入常规待排计算（恢复门店默认=恢复当月）
- 新增表：
  - `store_month_plans`：门店月度目标次数覆盖
- `visits` 扩展字段：
  - `type`：`regular`（常规）/ `extra`（临时）
  - `title`：临时上门原因（extra 必填）
  - `count_towards_target`：是否计入当月常规目标次数
  - `created_by`、`created_at`

### 接口变更
- 认证
  - `POST /api/auth/login` 签发 Token 有效期调整为 7 天
- 月度计划
  - `GET /api/store-month-plans?month=YYYY-MM`
  - `PUT /api/stores/:id/month-plan`（Admin）
  - `DELETE /api/stores/:id/month-plan?month=YYYY-MM`（Admin）
- 门店停用/恢复
  - `DELETE /api/stores/:id`：语义改为停用门店（Admin，策略 A）
  - `POST /api/stores/:id/restore`：恢复门店（Admin）
- 排班/临时上门
  - `POST /api/visits` 支持 `type/title/countTowardsTarget`
  - 权限：普通用户仅允许创建/更新/删除 `expert_name=本人` 的 visits

### 影响范围
- 前端排班页：待排门店计算、日历卡片样式、临时上门创建入口
- 前端门店管理页：新增“本月计划”入口；删除按钮语义变为停用并支持恢复
- 后端：外键约束强制开启（`foreign_keys=ON`）；新增/扩展表与接口
- 文档：已同步更新架构文档的 Schema 与 API 权限矩阵

### 验收要点（推荐顺序）
- 账号切换后默认展示本人日历视图；管理员可切换查看其他专家视图
- 管理员对某门店设置本月目标次数后，待排列表与进度按覆盖值变化（含目标为 0）
- 创建临时上门后：
  - 日历中显示为橙色卡片/标签
  - 默认不计入当月常规目标次数
- 停用策略 A：
  - 门店存在未来 planned 时，停用被阻止并提示原因
  - 取消/改期后可停用；停用后不可新建排班/临时上门；可恢复

### 模块上线
- 工作台 Module2 “终端监测报告”上线：入口 `/workspace/terminal-report/`，使用 Streamlit 生成并下载 PDF 报告；Nginx 配置上传总大小限制 10MB
